# ML Service Configuration
# Note: this service reads these from environment variables (it does not load .env automatically)

# Server Settings
ML_SERVICE_HOST=0.0.0.0
ML_SERVICE_PORT=8000

# MongoDB Connection (same database as the backend; used by the training scripts)
MONGO_URI=mongodb://localhost:27017/bedmanager

# Logging
LOG_LEVEL=INFO
